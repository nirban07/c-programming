#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
    // int a,b ;
    // printf("Give an integer a : ");
    // scanf("%d", &a);
    // printf("Give an integer b : ");
    // scanf("%d", &b);
    // printf("the sum of the given two integers is : %d\n",(a+b));
    // printf("Give two integers a and b : ");
    // scanf("%d%d",&a,&b);
    // printf("a and b :%d%d",a,b);

// Multiply two floating point numbers

    // double a,b, product;
    // printf("Enter two decimal numbers :");
    // scanf("%lf %lf",&a,&b);
    // product = a * b;
    // printf("The multiplied sum is: %lf",product);
    
    // double a, b, product;
    // printf("Enter two numbers: ");
    // scanf("%lf %lf", &a, &b);  
 
    // // Calculating product
    // product = a * b;

    // // %.2lf displays number up to 2 decimal point
    // printf("Product = %.2lf", product);


// Swapping of two numbers without using temp variable
    // double a, b;
    // printf("Enter a: ");
    // scanf("%lf", &a);
    // printf("Enter b: ");
    // scanf("%lf", &b);

    // // swapping

    // // a = (initial_a - initial_b)
    // a = a - b;   

    // // b = (initial_a - initial_b) + initial_b = initial_a
    // b = a + b;

    // // a = initial_a - (initial_a - initial_b) = initial_b
    // a = b - a;

    // // %.2lf displays numbers up to 2 decimal places
    // printf("After swapping, a = %.2lf\n", a);
    // printf("After swapping, b = %.2lf", b);

    // int a=5,b=2,c;
    // c = a%b;
    // printf("%d",c);
    // int *p ;
    // int c=2;
    // p=&c;
    // printf("c is %d\n",c);
    // printf("address of c : %d\n",p);
    // printf("address of p:%p",&p);
    // printf("address of c is %p\n",*p);
    // printf("address of c[0] :%p\n ",&c[0]);
    // printf("*c is %d\n",*c);
    



    return 0;
}